metadata:
  labels:
    agent: docker
spec:
  containers:
  - name: jnlp
  - name: docker
    image: docker:latest    
    command:
    - sleep
    tty: true
    env:
    - name: CONTAINER_ENV_VAR
      value: docker